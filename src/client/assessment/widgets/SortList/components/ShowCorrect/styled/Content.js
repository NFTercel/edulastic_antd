import styled from "styled-components";
import { FlexRow } from "./FlexRow";

export const Content = styled(FlexRow)`
  align-items: center;
  padding-right: 36px;
  padding-left: 36px;
  white-space: nowrap;
`;
