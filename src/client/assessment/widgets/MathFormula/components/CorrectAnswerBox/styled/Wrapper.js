import styled from "styled-components";

export const Wrapper = styled.div`
  padding: 15px;
  background: ${props => props.theme.widgets.mathFormula.answerWrapperBgColor};
  border-radius: 5px;
  margin: 15px 0;
`;
